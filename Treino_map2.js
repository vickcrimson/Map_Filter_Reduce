function mapSemThis(arr) {
   return arr.map(function(item){
        return item * 2
    }
   )}

const nums=[2,4,6,8,10];
console.log(mapSemThis(nums));
// a função retorna que todos os itens do array sejam multiplicados por dois